# 运行时依赖
# ------------------------------------------------------------------
# 桌面壳层用 pywebview（系统 WebView 内核），这是唯一一个运行时第三方依赖。
# 网络/JSON/并发等一律用标准库，不再引 requests 之类 ——
# 依赖越少，PyInstaller 三平台构建越稳、产物越小。
pywebview>=4.0

# 只有构建才需要：
pyinstaller>=6.0
